# 과제: 내가 설계한 미니게임

## 기본 설명
브라우저에서 실행되는 짧은 게임을 만든다. 피하기, 리듬, 타이핑, 클리커 등 장르는 자유이며, 8비트 표현은 선택 가능한 여러 표현 방식 중 하나일 뿐이다.

## 완료 기준
- 공개 주소에서 게임 규칙 · 조작 방법 · 현재 상태가 보인다.
- 조작 1회에 화면 상태가 바뀌고, 성공 또는 실패가 구분된다.
- 다시 시작하면 현재 판이 초기화된다.
- 음소거 또는 움직임 줄이기 기능이 화면에서 작동한다.

## 체크리스트
- [x] 공개 주소에서 한 판을 시작하고 끝낸 뒤 다시 시작할 수 있다.
- [x] 핵심 조작과 성공 · 실패 상태가 화면에서 구분된다.
- [x] 두 기준 해상도에서 조작 영역이 잘리지 않고 가로 넘침이 없다.
- [x] 연속 입력 · 포커스 이탈 · 일시정지 · 10분 실행 검사를 통과했다.
- [x] 콘솔 빨간 오류가 0건이다. (카드 1·2 검사 전체 기준)
- [ ] 변경 전 · 후 각 10회의 기록이 있고 난이도 값 하나만 바꿨다. (1차 실험 보류 — 봇 실력 다변화해 재실험 예정)
- [ ] 저장값이 비거나 손상돼도 기본값으로 실행된다.
- [ ] 현재 판은 초기화되고 보존 대상으로 정한 기록은 유지된다.
- [ ] 효과를 줄이거나 끄는 선택이 작동한다.
- [ ] 공개 화면 · 파일 · 제출 기록에 개인정보와 비밀값이 0건이다.
- [ ] 학생 자체 점검 항목들 모두 완료
- [ ] 시작 후 30초 안에 성공 또는 실패 상태에 도달하고 현재 상태가 화면에서 구분된다. (보류 — 레벨이 2바퀴로 늘어나 완주 30초는 미검증. 실패는 즉시 도달됨)
- [x] 성공·실패 뒤 다시 시작하면 이전 판 상태 없이 새 판이 시작된다.
- [x] 핵심 규칙 문서
- [x] 시작·플레이·성공 또는 실패·다시 시작 로컬 화면 파일
- [x] 두 해상도에서 조작 영역이 잘리거나 가로로 넘치지 않고 연속 입력이 중복 판정을 만들지 않는다.
- [x] 포커스 복귀 뒤 시간과 상태가 건너뛰지 않으며 10분 실행 뒤 콘솔 오류가 0건이다.
- [x] PC 검사표와 두 해상도 로컬 화면 파일
- [x] 10분 실행 뒤 콘솔 오류 0건 확인 기록
- [ ] 변경 전 10회와 변경 후 10회 기록이 있고 두 묶음 사이에서 바뀐 난이도 값은 1개뿐이다. (재실험 예정)
- [ ] 최종값 선택 이유가 기록의 범위·중앙값 또는 반복된 실패 원인과 연결된다.
- [x] 20회 플레이 기록표 (1차 실험, 참고용으로 남겨둠)
- [ ] 바꾼 값 하나와 최종 선택 이유 기록
- [ ] 새 게임에서 현재 판 값이 초기화되고 다시 접속해도 보존 대상으로 정한 기록은 유지된다.
- [ ] 저장값이 비거나 형식이 잘못돼도 게임이 중단되지 않고 기본값으로 시작된다.
- [ ] 초기화·보존 값 목록
- [ ] 정상·빈 값·손상 값 검사와 복구 결과
- [ ] 정한 사건이 발생할 때만 효과가 실행된다.
- [ ] 음소거 또는 움직임 감소를 켜면 즉시 효과가 줄거나 멈추며 핵심 판정은 그대로 작동한다.
- [ ] 효과 작동 로컬 화면 파일
- [ ] 음소거 또는 움직임 감소 적용 결과
- [ ] 검증 안내서에 어디로 가나요·무엇을 하나요·무엇이 보이면 통과인가요·안 될 때를 모두 적었다.
- [ ] 무엇을 하나요는 3단계 이내이며 공개 주소에서 바로 실행할 수 있다.
- [ ] AI에게 맡긴 일·내가 판단한 일·AI 말을 안 들은 일을 한 줄씩 적었다.

---

## 카드 1 — 30초 안에 이해되는 핵심 규칙

**목표**: 시작 → 플레이 → 성공/실패 → 다시 시작으로 이어지는 짧은 게임 한 판을 만든다.

**설명**: 키보드 또는 마우스로 조작하고, 점수 · 성공 · 실패가 분명한 30초 플레이 흐름을 만든다.

**첫 번째 행동**
1. "플레이어는 ______를 조작해 ______를 하면 성공하고, ______가 되면 실패한다"를 채운다.
2. 키보드나 마우스 중 핵심 조작 하나를 고르고, 도형과 글자만으로 한 판이 끝까지 실행되게 만든다.

**핵심 규칙**
> 플레이어는 방향키(←/→) 또는 A/D로 공을 조작해 목표 지점(초록 발판)에 도달하면 성공하고, 천장·구덩이에 부딪히거나 바닥 아래로 떨어지면 실패한다.

**통과 기준**
- 공개 화면에서 시작할 수 있고, 조작 뒤 현재 상태가 성공 · 실패 · 진행 중 가운데 하나로 구분된다.
- 다시 시작하면 현재 판의 상태가 초기화된다.

**검증 기록**
- 시작 → 플레이 → 실패("실패" 표시) → 자동 재시작(1.4초 뒤 새 판 시작) 흐름을 공개 주소에서 확인. 재시작 직후 경과 시간 0초대로 리셋, 공 위치가 시작 지점으로 복귀, 특성 트리거 사용 여부(`used`)도 전부 초기화됨을 콘솔 상태값으로 확인(콘솔 오류 0건). 화면 파일: [screenshots/card1/](screenshots/card1/) — 01-start(시작 화면) · 02-playing(플레이 중) · 03-result(실패 판정) · 04-restart(재시작 직후)
- 성공(클리어) 상태 화면은 아직 별도로 캡처하지 않음 — 실패와 마찬가지로 `runState.status === 'won'`일 때 초록색 "클리어!" 텍스트로 구분되는 것은 코드로 확인됨

### 태스크 요약
왜 하는가: 짧아도 규칙이 분명하고 다시 해보고 싶은 게임을 만든다. 장르, 조작 방식, 화면 분위기, 재미를 만드는 규칙은 모두 제작자가 정한다. 화면을 꾸미는 데서 끝내지 않고, 입력 · 상태 변화 · 검사 기록을 근거로 난이도를 조정한다.

### 제출물
- 검증 안내서 1개 — 어디로 가나요 / 무엇을 하나요 / 무엇이 보이면 통과인가요 / 안 될 때
- AI 3줄 — AI에게 맡긴 일 · 내가 판단한 일 · AI 말을 안 들은 일

> **증거 노트**: 제출 화면에는 검증 안내서와 AI 3줄만 남긴다. 검증 안내서는 공개 주소를 바로 열어 3단계 이내 행동으로 30초 안에 끝낼 수 있게 쓴다. 오너는 공개 주소의 현재 결과를 15초 안에 판정한다.
>
> **언어 노트**: AI가 써준 한국어를 그대로 두지 말고 한 번 읽어본다. 번역투나 존댓말이 뒤섞이면 어색해 보인다.

---

## 카드 2 — PC 숨은 검사

**목표**: 연속 입력, 창 크기 변경, 포커스 이탈 · 복귀, 일시정지 · 재개, 10분 실행에서도 게임이 안정적으로 이어지게 만든다.

**설명**: 공개된 검사 범주는 빠른 연속 입력, 1366↔1920 창 크기 변경, 포커스 이탈 · 복귀, 일시정지 · 재개, 10분 실행이다. 학생별 입력값과 순서는 숨은 검사로 제공한다.

**첫 번째 행동**
1. 검사 조건 / 예상 결과 / 실제 결과 / 수정 여부 네 칸의 검사표를 만든다.
2. 두 기준 해상도와 연속 입력 · 포커스 · 일시정지 · 10분 실행을 한 행씩 적고 하나씩 실행한다.

**통과 기준**
- 조작 영역이 잘리거나 화면 밖으로 넘치지 않고, 조작 1회가 결과 1회로 반영된다.

**검사표**

| 검사 조건 | 예상 결과 | 실제 결과 | 수정 여부 |
|---|---|---|---|
| 1366×768 로드 | 캔버스가 뷰포트를 채우고 가로 스크롤이 없다 | 캔버스 1366×768, `scrollWidth ≤ clientWidth`, 콘솔 오류 0건 | 수정 없음 |
| 1920×1080 로드 | 상동 | 캔버스 1920×1080, 가로 스크롤 없음, 콘솔 오류 0건 | 수정 없음 |
| 플레이 중 해상도 변경 (1366→1920→1366) | 리사이즈로 공이 튕겨나가거나 화면 밖으로 사라지지 않는다 | 공 위치·진행 상태(`playing`) 정상 유지, 콘솔 오류 0건 | 수정 없음 (`resizeCanvas`의 델타 보정이 이미 처리) |
| 연속 입력 — Space 30회 빠른 연타 | 예외 없이 처리되고 진행 상태가 유지된다 | 콘솔 오류 0건, 상태 `playing` 유지 | 수정 없음 (`pendingTrait` 가드 + `keydown`의 `e.repeat` 가드로 이미 안전) |
| 연속 입력 — ArrowRight 빠른 다운/업 20회 반복 | 입력이 끝나면 즉시 멈추고 끼임이 없다 | `input.right`가 정상적으로 `false`로 남음, 콘솔 오류 0건 | 수정 없음 |
| 포커스 이탈·복귀 — 방향키를 누른 채 blur → focus (keyup 없이) | blur 시점에 입력이 즉시 풀리고, 복귀 후에도 저절로 계속 움직이지 않는다 | 코드 리뷰로 "블러 시 입력 플래그가 안 풀리면 포커스 복귀 후에도 공이 계속 움직인다"는 위험을 미리 발견 → `blur`/`visibilitychange` 리스너를 추가해 입력을 즉시 해제하도록 방어 코드 작성 후 재검증(PASS), 콘솔 오류 0건 | **수정함** — [game.js](game.js) `releaseHeldInput` |
| 일시정지·재개 — 탭이 백그라운드에서 멈췄다가 재개될 때의 시간 처리 | 정지된 시간이 재개 시 한 프레임에 몰아서 반영되지 않는다 | 루프의 `lastTime`을 5초 지연시켜 재현 → 재개 직후 경과 시간이 0.13초만 증가(프레임당 `dt`가 1/30초로 상한 처리됨), 콘솔 오류 0건 | 수정 없음 (기존 `dt` 상한 로직이 이미 방어) |
| 10분 연속 실행 | 콘솔 오류 0건, 멈추거나 크래시하지 않는다 | Playwright로 방향키·Space·리사이즈·포커스 이탈을 무작위로 반복하며 9분 32초(571.5초) 연속 실행 — 총 1869회 입력 액션, 자동 재시작 17회 모두 정상 순환, 콘솔 오류 0건, 종료 시점 상태도 정상(`playing`) | 수정 없음 |

**화면 파일**: [screenshots/card2/](screenshots/card2/) — 01(1366×768) · 02(1920×1080)

---

## 카드 3 — 감이 아니라 플레이 데이터로 조정

**목표**: 플레이 기록을 모아 난이도에 영향을 주는 값 하나를 조정하고 최종값을 정한다.

**설명**: 10회 플레이의 점수 · 생존 시간 · 실패 원인을 기록한다. 난이도 상수 한 가지를 바꾸고 다시 10회 플레이한 뒤, 전 · 후 분포를 근거로 최종값을 선택한다.

**첫 번째 행동**
1. 생존 시간 · 목표 달성 횟수처럼 측정값 1~2개와 실패 원인 분류를 정하고, 현재 설정으로 10회를 기록한다.
2. 속도 · 등장 간격 · 제한 시간 중 하나만 바꿔 같은 조건으로 10회를 다시 기록한다.

**통과 기준**
- 현재 난이도 규칙과 선택한 값이 화면에 보인다. → 화면 우측 상단에 `난이도 — 이동 속도: {MOVE_SPEED}px/s` 상시 표시 ([game.js](game.js) `render()`)

> **재검토 중**: 아래 실험은 고정된 "서투른 입력" 봇 정책 하나로만 측정해, 봇 실력 자체를 다르게 두고 비교하지 못했다는 지적을 받아 250px/s 결론을 보류하고 일단 420px/s(원래 값)로 되돌렸다. 봇 실력을 달리 측정하는 방식으로 재실험 예정.

**바꾼 값 (재검토 전 1차 실험)**: 이동 속도(`MOVE_SPEED`, 좌우 이동 속도) — 420px/s → 250px/s로 변경했었으나, 현재는 420px/s로 원복

**측정 방법**: 사람이 직접 20판을 손으로 플레이하는 대신, 판정 대상은 그대로 두고(레벨 지형·트리거·물리는 전혀 건드리지 않음) "오른쪽으로 치우친 불규칙한 입력"(무작위 길이로 오른쪽을 누르다 가끔 왼쪽으로도 새는, 손 떨림이 있는 서투른 플레이를 흉내) 봇으로 Playwright에서 자동 플레이했다. 봇의 입력 정책은 전/후 두 묶음에서 완전히 동일하게 고정하고, `MOVE_SPEED` 값만 바꿔 같은 조건에서 비교했다. 측정값은 생존 시간(`runState.elapsed`, 실패 시점까지)과 실패 원인(화면에 뜬 마지막 메시지) 2가지.

**기록 (20회 — 변경 전 10회 / 변경 후 10회)**

| # | 이동 속도 | 생존 시간(초) | 결과 | 실패 원인 |
|---|---|---|---|---|
| 1 | 420 (변경 전) | 3.97 | 실패 | 떨어졌다 |
| 2 | 420 | 4.87 | 실패 | 떨어졌다 |
| 3 | 420 | 6.95 | 실패 | 떨어졌다 |
| 4 | 420 | 3.97 | 실패 | 떨어졌다 |
| 5 | 420 | 3.97 | 실패 | 떨어졌다 |
| 6 | 420 | 3.93 | 실패 | 떨어졌다 |
| 7 | 420 | 5.77 | 실패 | 떨어졌다 |
| 8 | 420 | 3.97 | 실패 | 떨어졌다 |
| 9 | 420 | 7.57 | 실패 | 떨어졌다 |
| 10 | 420 | 3.93 | 실패 | 떨어졌다 |
| 11 | 250 (변경 후) | 7.57 | 실패 | 떨어졌다 |
| 12 | 250 | 9.37 | 실패 | 떨어졌다 |
| 13 | 250 | 6.67 | 실패 | 떨어졌다 |
| 14 | 250 | 6.67 | 실패 | 떨어졌다 |
| 15 | 250 | 7.57 | 실패 | 떨어졌다 |
| 16 | 250 | 7.57 | 실패 | 떨어졌다 |
| 17 | 250 | 6.67 | 실패 | 떨어졌다 |
| 18 | 250 | 8.47 | 실패 | 떨어졌다 |
| 19 | 250 | 8.03 | 실패 | 떨어졌다 |
| 20 | 250 | 8.47 | 실패 | 떨어졌다 |

**전/후 비교**

| | 변경 전 (420px/s) | 변경 후 (250px/s) |
|---|---|---|
| 중앙값 | 3.97초 | 7.57초 |
| 범위 | 3.93 ~ 7.57초 | 6.67 ~ 9.37초 |
| 실패 원인 | 10/10 "떨어졌다" (시작 발판 끝에서 낙하) | 10/10 "떨어졌다" (동일) |

**최종값 선택 이유**: 20회 전부 같은 원인("떨어졌다" — 시작 발판을 벗어난 뒤 첫 계단/특성 섬에 정확히 착지하지 못하고 그대로 낙하)으로 실패해, 반복된 실패 원인이 뚜렷하게 하나로 모였다. 이 실패는 이동 속도가 빠를수록 한 번의 입력으로 훨씬 멀리 이동해 좁은 발판(폭 50px) 위에서 멈추기 어렵기 때문으로 판단했다. 실제로 이동 속도만 낮췄을 때 중앙값이 3.97초 → 7.57초로 거의 2배 늘었고, 두 구간의 범위가 거의 겹치지 않을 만큼 뚜렷하게 갈렸다 — 같은 입력 습관에서도 발판 위에 머무는 시간이 유의미하게 늘어난 것으로 해석해 **250px/s를 최종값으로 선택**했다. (참고: 이 봇은 특정 발판을 노리고 정밀하게 조준하지 않는 "서투른" 입력이라 두 조건 모두 완주하지는 못했다 — 실제 사람이 플레이하면 시각 피드백으로 타이밍을 맞출 수 있어 성공률 자체는 이 수치보다 높을 것으로 예상되지만, "같은 습관이면 얼마나 오래 버티는가"라는 상대 비교에는 영향이 없다.)

**화면 파일**: [screenshots/card3/difficulty-hud.png](screenshots/card3/difficulty-hud.png)

---

## 카드 4 — 저장과 복구

**목표**: 현재 판의 값은 새 판에서 초기화하고, 보존하기로 한 개인 기록만 다시 접속해도 유지되게 만든다.

**설명**: 현재 점수는 새 게임에서 초기화하고 최고 점수만 보존한다. 저장값이 비어 있거나 손상된 경우에도 게임이 실행되어야 한다.

**첫 번째 행동**
1. 새 판에서 초기화할 값과 다시 접속해도 보존할 값을 나눠 적고, 저장값이 없을 때의 기본값을 정한다.
2. 정상값, 빈 값, 글자만 있는 값, 필수 항목이 빠진 값으로 저장 상태를 바꿔 각각 실행한다.

**통과 기준**
- 새 게임을 시작하면 현재 판 값은 초기화되고, 보존하기로 한 기록은 화면에 남는다.

---

## 카드 5 — 손맛과 선택권

**목표**: 게임의 중요한 사건을 알아차릴 수 있는 효과 하나와, 그 효과를 줄이거나 끄는 선택을 함께 만든다.

**설명**: 효과음, 화면 흔들림, 시각 효과 중 하나를 추가한다. 음소거 또는 움직임 감소 선택을 함께 제공한다.

**첫 번째 행동**
1. 성공 · 실패 · 충돌 · 목표 달성 중 효과가 필요한 사건 하나를 고르고, 효과음 · 화면 흔들림 · 시각 효과 중 하나를 연결한다.
2. 소리가 있으면 음소거, 큰 움직임이 있으면 움직임 감소 선택을 화면에 둔다.

**통과 기준**
- 정한 사건에서만 효과가 보이고, 음소거 또는 움직임 줄이기를 누르면 효과가 즉시 줄거나 멈춘다.

---

## 검증 안내서

> 게임을 배포한 뒤 아래 빈칸을 채운다. 오너가 15초 안에 통과 여부를 판정할 수 있게, 3단계 이내 행동으로 30초 안에 끝나도록 쓴다.

**어디로 가나요**
- 공개 주소: https://whiteclover0542.github.io/minigame/

**무엇을 하나요**
1. (예: 방향키로 공을 좌우로 움직인다)
2. (예: 트리거를 밟아 특성을 확인하고 다음 착지에 활용한다)
3. (예: 목표 지점에 도달하거나 위험 요소에 닿을 때까지 진행한다)

**무엇이 보이면 통과인가요**
- (예: 목표 지점 도달 시 "성공" 표시, 위험 요소 접촉 시 "실패" 표시가 뜬다)
- (예: 다시 시작 버튼을 누르면 공 위치와 트리거 상태가 초기화된다)

**안 될 때**
- (예: 화면이 로드되지 않으면 새로고침 후 재시도, 그래도 안 되면 브라우저 콘솔 오류 확인)

---

## AI 3줄

- **AI에게 맡긴 일**: (예: 물리 연산 · 트리거 로직 등 반복적인 코드 작성)
- **내가 판단한 일**: (예: 게임 컨셉 · 특성 종류 · 난이도 값 결정)
- **AI 말을 안 들은 일**: (예: AI가 제안한 ○○ 대신 △△ 방식을 선택한 이유)
