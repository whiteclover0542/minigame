# 바운스볼 트리거 퍼즐 — 과제 제출 정리

## 개요

- **게임**: 바운스볼 트리거 퍼즐 — 공이 자동으로 계속 튀어 오르는 상태에서, 좌우 이동만으로 트리거 섬에 착지해 특성(구름·탱탱볼·철구슬·얼음)을 얻고, 그 특성으로 5개 구간을 통과해 목표 지점에 도달하는 물리 퍼즐.
- **공개 주소**: https://whiteclover0542.github.io/minigame/
- **완료 기준**: 공개 주소에서 규칙·조작·상태 확인 가능 / 조작 1회에 상태 변화·성공·실패 구분 / 재시작 시 현재 판 초기화 / 음소거·움직임 감소 작동 — 4개 전부 충족.

---

## 검증 안내서

**어디로 가나요**
- 공개 주소: https://whiteclover0542.github.io/minigame/

**무엇을 하나요**
1. 방향키(←/→) 또는 A/D로 공을 움직여 발판 위 특성 섬에 착지한다
2. 특성을 얻으면 Space로 발동해 다음 장애물을 통과한다
3. 초록 발판(목표)에 닿거나 천장·구덩이·바닥으로 떨어질 때까지 진행한다

**무엇이 보이면 통과인가요**
- 목표 지점 도달 시 초록색 "클리어!" 표시, 장애물 충돌·추락 시 빨간색 "실패" 표시가 뜬다
- 실패 후 약 1.4초 뒤 공 위치·시간·특성 상태가 전부 초기화된 새 판이 자동으로 시작된다

**안 될 때**
- 화면이 로드되지 않으면 새로고침 후 재시도, 그래도 안 되면 브라우저 콘솔에 빨간 오류가 있는지 확인

---

## AI 3줄

- **AI에게 맡긴 일**: 물리 연산·충돌 판정·트리거 좌표 계산 같은 반복적인 코드 작성, Playwright로 자동 플레이시켜 버그 찾고 데이터 모으는 것까지 대부분 맡겼다.
- **내가 판단한 일**: 게임 컨셉과 특성 4종, 레벨을 몇 구간으로 만들지, 일시정지 버튼을 어디에 어떻게 보이게 할지 같은 디자인 방향은 직접 정하고 계속 피드백을 줘서 고쳤다.
- **AI 말을 안 들은 일**: AI가 봇 하나로만 테스트해서 "이동 속도를 250으로 낮추는 게 낫다"고 결론 냈을 때, 봇 실력이 하나뿐이라 근거가 약하다고 판단해 실력을 여러 단계로 나눠 다시 실험시켰다. 결과가 뒤집혀서 원래 값(420)을 그대로 쓰기로 했다.

---

## 카드별 완료 요약

### 카드 1 — 30초 안에 이해되는 핵심 규칙 ✅
- **핵심 규칙**: 방향키/A·D로 공을 조작해 목표(초록 발판) 도달 시 성공, 천장·구덩이·추락 시 실패한다.
- 공개 주소에서 시작→플레이→실패→자동 재시작(1.4초 후, 상태 전부 초기화) 확인.
- **30초 내 완주**: 실사용자가 직접 플레이해 **23.8초**에 클리어 — 기준 충족.

### 카드 2 — PC 숨은 검사 ✅

| 검사 조건 | 결과 |
|---|---|
| 1366×768 / 1920×1080 로드 및 리사이즈 | 스크롤·잘림 없음, 오류 0건 |
| Space 연타 / 방향키 빠른 다운업 | 중복 판정 없음 |
| 포커스 이탈·복귀 | 버그 발견(입력 끼임) → 수정 완료 |
| 일시정지·재개(백그라운드 정지 재현) | dt 상한으로 시간 점프 없음 |
| 10분 연속 실행 | 1869회 입력, 재시작 17회, 오류 0건 |

### 카드 3 — 감이 아니라 플레이 데이터로 조정 ✅
- **바꾼 값**: 이동 속도(`MOVE_SPEED`) — 250, 300을 후보로 테스트, **420(원래 값) 유지**로 결정.
- 실력 4단계(초급~고수) 조준 봇으로 420 vs 300을 각 20회씩 총 40회 비교.
- 420에서는 실력이 오를수록 1관문 통과율 상승(2/5→2/5→2/5→**4/5**), 300은 모든 단계에서 같거나 나빠져 원래 값을 데이터로 확정.

### 카드 4 — 저장과 복구 ✅
- **초기화 값**: 공 위치·속도, 특성 상태, 경과 시간 등 — 매 판 초기화.
- **보존 값**: 최고 클리어 기록(localStorage) — 새 판·재접속에도 유지.
- 저장값 없음/빈 문자열/손상 텍스트/필드 누락/타입 오류 등 8가지 상태 모두 기본값 또는 정상 복원 확인.

### 카드 5 — 손맛과 선택권 ✅
- **연결한 사건**: 실패(충돌·추락) → 화면 흔들림(0.35초 감쇠).
- **선택권**: 일시정지 메뉴의 "움직임 감소" 버튼 — 켜면 흔들림 즉시 정지, 판정 로직은 영향 없음.
- 부가 기능: Esc 일시정지, 일시정지 중 "게임 설명 보기"로 조작법 재확인 가능.

---

## 체크리스트 (11/11 완료)

- [x] 공개 주소에서 한 판을 시작하고 끝낸 뒤 다시 시작할 수 있다.
- [x] 핵심 조작과 성공·실패 상태가 화면에서 구분된다.
- [x] 두 기준 해상도에서 조작 영역이 잘리지 않고 가로 넘침이 없다.
- [x] 연속 입력·포커스 이탈·일시정지·10분 실행 검사를 통과했다.
- [x] 콘솔 빨간 오류가 0건이다.
- [x] 변경 전·후 각 10회의 기록이 있고 난이도 값 하나만 바꿨다.
- [x] 저장값이 비거나 손상돼도 기본값으로 실행된다.
- [x] 현재 판은 초기화되고 보존 대상으로 정한 기록은 유지된다.
- [x] 효과를 줄이거나 끄는 선택이 작동한다.
- [x] 공개 화면·파일·제출 기록에 개인정보와 비밀값이 0건이다.
- [x] 학생 자체 점검 항목들 모두 완료
